{
  "name": "monolith-desktop-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "postbuild": "cargo build --release --manifest-path src-tauri/Cargo.toml",
    "build:dev": "vite build && cargo build --manifest-path src-tauri/Cargo.toml",
    "dev": "vite",
    "tauri": "tauri",
    "tauri:dev": "tauri dev"
  },
  "dependencies": {
    "preact": "^10.24.0",
    "@preact/signals": "^1.3.0",
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.7.0",
    "@tauri-apps/cli": "^2"
  }
}
