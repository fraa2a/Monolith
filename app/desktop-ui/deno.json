{
  "$comment": "Monolith Desktop UI frontend build. Deno is used only as the esbuild bundler for the Preact frontend (build.ts → dist/); the app shell/host is Tauri (src-tauri/, cargo). dist/ is embedded into the exe via include_dir!. See ADR-0012.",
  "tasks": {
    "ui:build": "deno run -A build.ts"
  },
  "imports": {
    "@std/path": "jsr:@std/path@^1.0.0",
    "esbuild": "npm:esbuild@^0.24.0",
    "esbuild-deno-loader": "jsr:@luca/esbuild-deno-loader@^0.11.0",
    "preact": "npm:preact@^10.24.0",
    "preact/": "npm:/preact@^10.24.0/",
    "@preact/signals": "npm:@preact/signals@^1.3.0"
  },
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "preact"
  }
}
