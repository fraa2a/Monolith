# app/engine

Reserved for a future headless-engine split (recording/replay logic without
the Win32 tray/message-loop host). Deferred — see `CLAUDE.md`'s Architecture
section. `app/recorder` remains the single-process MVP for now.
