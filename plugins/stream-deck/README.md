# Stream Deck Plugin

This folder is reserved for the future Elgato Stream Deck plugin.

Plugin role: remote controller only — sends commands over WebSocket/TCP JSON-RPC, contains no recording logic.

Planned scope:

- `save_replay`
- `recording_start`
- `recording_stop`
- `pause_resume`

Status:

- Not implemented
- No SDK installed in this session
- Keep this isolated from the native app codebase

