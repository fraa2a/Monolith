# libs/config

Reserved for a future extraction of `app/recorder/src/settings_config.*` into
a standalone library, if/when `app/engine` (see `app/engine/README.md`) needs
config loading independent of the recorder host. Deferred; settings loading
currently lives in `app/recorder/src/settings_config.cpp`.
